<article>
    <h1><%= @article.title %></h1>
    <p class="date">投稿日：<%= @article.created_at.strftime("%Y年 %m月 %d日 %H%M") %></p>

    <div class="content">
        <%= simple_format(@article.content) %>
    </div>
</article>

<div class="actions">
    <%= link_to "記事一覧へ", articles_path %>
</div>