<h1>My Blog</h1>
<p>Ruby on Railsで作るブログ</p>

<div class="recent-articles">
  <h2>最新の記事</h2>
  <% if @recent_articles.any? %>
    <ul>
      <% @recent_articles do |article| %>
        <li>
          <%= link_to article.title, article_path(article) %>
          <small>(<%= article.created_at.strftime("%m/%d") %>)</small>
        </li>
      <% end %>
    </ul>
    <%= link_to "全ての記事を見る →", article_path %>
    <% else %>
      <p>まだ記事がありません。</p>
        <%= link_to "最初の記事を書く", new_article_path %>
    <% end %>
</div>